import { Routes } from '@angular/router';
import { WarehouseComponent } from './werehouse/werehouse.component';

export const routes: Routes = [
  { path: 'warehouse', component: WarehouseComponent }, // ✅ correct

];
